apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # - deployment.yaml
  # - service.yaml
  # - storage.yaml
configMapGenerator:
  - name: audiobookshelf-config
    literals:
      - PORT=80
